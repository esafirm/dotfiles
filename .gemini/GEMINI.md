## Gemini Added Memories

- Always exclude the 'build' directory from searches and file operations.
- Always using git grep to search for code if possible, otherwise fallback to regular grep.
- Always exclude the 'build' directory from 'grep', 'find', or any recursive file searches.
